"""IMPORT-ONLY STUB for the shareable validation package.

The real module lives in stanford-swepr/ai-artifacts-collector. It is imported
at the top of maturity_scorer.py but none of its functions are reached by
notebook 15.
"""

DEFAULT_TASK_PREFIX = "classification: "  # placeholder default-arg value; never used


def generate_embeddings_batch(*args, **kwargs):
    raise NotImplementedError(
        "generate_embeddings_batch is stubbed in the shareable validation "
        "package — requires the ai-artifacts-collector repo (see README.md)")
