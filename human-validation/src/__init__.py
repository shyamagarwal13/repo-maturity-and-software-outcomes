"""Shareable validation package — trimmed src/.

`maturity_scorer.py` and `artifact_filtering.py` are verbatim copies from the
ai-artifacts-analyzer repo. The two collector-repo modules it imports are
provided as import-only stubs (see their headers); notebook 15 never calls
the stubbed functions.
"""
